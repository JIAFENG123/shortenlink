{
  "name": "shortenlink",
  "version": "2.0.0",
  "private": true,
  "description": "A simple URL shortener deployed on Netlify",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo \"No build step required\""
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.36.1"
  }
}
